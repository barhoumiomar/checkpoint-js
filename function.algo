procedure tri_A_bulle(arr= array )
    n := length(arr) // te5ou toull t3 liste
    for i from 1 to n - 1 do // ta3ml parcour t3 liste
        pos := arr[i] // key te5odh position t3 i
        j := i - 1 // j tparcouri el position li 9bal i
        while (j >= 0 and arr[j] > key) do // 3mlt condition bch ysir comparaison bin el  case w jaretha 
            arr[j + 1] := arr[j] //lenna tbadlo 
            j=j-1
            arr[j + 1] := pos //  lenna compteur y9adm 
end procedure
